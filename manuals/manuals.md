# 環境構築メモ

```bash
# ESLintとprettierのインストール
npm install -D eslint prettier eslint-config-prettier eslint-plugin-prettier
# react-hook-form と zod のインストール
npm install react-hook-form @hookform/resolvers zod
```
